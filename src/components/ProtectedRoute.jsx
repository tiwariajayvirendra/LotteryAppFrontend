 import React from "react";

/**
 * ProtectedRoute component without any token/auth checks.
 * Simply renders its children.
 */
const ProtectedRoute = ({ children }) => {
  return children;
};

export default ProtectedRoute;
